1、新增一个服务提供者，预期 实例新增通知
2、下线一个服务提供者，预期 实现下线通知
3、conf 节点变化，预期 收到 服务配置变化通知
4、路由变化：
1）不在路由规则中，提供者被加入到一个 only=N 的路由规则
预期：不会引起实例变化通知
2）不在路由规则中，提供者被加入到一个 only=Y 的路由规则中
预期：收到实例下线的通知
3）在路由规则中，路由规则新增一个存在的 服务实例
预期： 收到实例上线通知
4）在路由规则中，路由规则删除一个存在的 服务实例
预期：收到实例下线通知
5）从不在路由规则，调整到路由规则。
预期：之前非路由规则的实例下线，路由规则中的实例上线
6）从路由规则中，调整到非路由规则中
预期：路由规则中的实例下线，路由规则外的实例上线
5、实例配置变化
1）是否可用 由 Y--》N
预期：下线
2）是否可用 由N --》Y
预期：上线
